<div class="container">
  <div class="row p-2">
    <div class="col-md-12">
      <h1>Courses</h1>
    </div>
  </div>
  <div class="row align-items-center p-2" >
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>Name</th>
          <th>Description</th>
          <th>Type</th>
          <th>Actions</th>
        </tr>
      <% @courses.each do |r| %>
        <tr>
          <td><%= link_to r.title, r %></td>
          <td style="width: 40%"><%= truncate(r.description, length: 25, separator: ' ')%></td>
          <td><%= r.course_type %></td>
          <td>
            <%= link_to 'Edit', edit_course_path(r), class: "btn btn-primary" %>
            <%= link_to 'Delete', course_path(r), method: :delete, data: { confirm: 'Are you sure?' }, class: "btn btn-danger" %>
          </td>

        </tr>
      <% end %>
    </table>
</div>